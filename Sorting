import java.util.Comparator;
import java.util.Collections;
import java.util.ArrayList;

public class Sorting
{
	public static void Pelicula
	{

	private String Tienda.IngresarPelicula().Titulo;
	private String PeliculDesc;
	private int quantity;

	public String getTienda.IngresarPelicula().Titulo() 
	{
		return Tienda.IngresarPelicula().Titulo;
	}
	
	public void setTienda.IngresarPelicula().Titulo(String Tienda.IngresarPelicula().Titulo) 
	{
		this.Tienda.IngresarPelicula().Titulo = Tienda.IngresarPelicula().Titulo;
	}
	
	public String getPeliculaDesc() 
	{
		return PeliculaDesc;
	}
	
	public void setPeliculaDesc(String PeliculaDesc) 
	{
		this.PeliculaDesc = PeliculaDesc;
	}
	
	public int getQuantity() 
	{
		return quantity;
	}
	
	public void setQuantity(int quantity) 
	{
		this.quantity = quantity;
	}
	
	}

	List<Pelicula>  Peliculas = new ArrayList<Fruit>();

	Pelicula pelicula;
	for(int i=0;i<100;i++)
	{
		Pelicula = new Pelicula();
		Pelicula.setname(...);
		Peliculas.add(Pelicula);
	}

}//Sorting

/*  No sé sí meterlo en esta parte de pelicula

    public void mostrar(String texto) {
		this.superMostrar(texto);
		MCursor.TCen("============================================================");
		MCursor.TJus("       DIRECTOR = " + this.director);
		this.protagonistas.forEach(e -> {
			MCursor.TJus("   PROTAGONISTA = " + e);
		});
		MCursor.TCen("============================================================");
		MCursor.Salida();
	}//Mostrar
  
  O ponerlo en esta otra de tienda
  
  public static void mostrarPelicula() {
		if(PELICULAS.isEmpty()) error("NO HAY PELICULAS ALMACENADAS");
		else {
			for(int i = 0; i < PELICULAS.size(); i++) {
				MCursor.BLinea(30);
				PELICULAS.get(i).mostrar("PELICULAS NUMERO " + (i + 1));
			}//for
		}//else
		return;
	}
*/
